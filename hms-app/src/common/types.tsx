export type Idea = {
  id: number
  title: string
  description: string
  reason: string
  problem: string
  goal: string
  skills: string[]
  category: string
  participants: string[]
  aws: boolean
}
