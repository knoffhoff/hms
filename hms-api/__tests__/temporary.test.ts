'use strict';

import {hackathonIds} from '../src/mock/hackathon';

test('A useless example test just to have how tests work for now.', () => {
  console.log(JSON.stringify(hackathonIds));

  expect(2 * 9).toBe(18);
});
